CalCalc
--------

CalCalc contains a set of functions to evaluate any string passed to it, either through Python or through the Wolfram|Alpha API. It can be used from the command line or by importing within Python.

Installing
--------

To install CalCalc, download the source, set your working directory to the source directory and run the following:

python setup.py install 